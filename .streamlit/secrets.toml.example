# Copy to .streamlit/secrets.toml for local dev, or paste into Streamlit Cloud → Secrets.
# Only needed if you add auth, private APIs, or analytics later.

# [auth]
# cookie_secret = "generate-a-long-random-string"
# credentials = { usernames = { admin = { name = "Admin", password = "use-a-strong-hash" } } }

# [cms]
# api_key = ""  # CMS Hospital Compare is public — no key required today